[pytest]
DJANGO_SETTINGS_MODULE = reeltalk.settings
python_files = tests.py test_*.py *_tests.py
addopts = --cov=reeltalk --cov-config=.coveragerc
testpaths = reeltalk/tests
markers =
    integration: marks tests as requiring external resources (deselect with '-m "not integration"')

env =
    LANGUAGE_CODE = en-US
    SECRET_KEY = beepbeep
    DEBUG = false
    DOMAIN = your.domain.here
    PORT = 4242
    ALLOWED_HOSTS = your.domain.here
    REELTALK_DATABASE_BACKEND = postgres
    MEDIA_ROOT = images/
    CELERY_BROKER_URL = memory://
    CELERY_RESULT_BACKEND = cache+memory://
    USE_DUMMY_CACHE = true
    EMAIL_HOST = smtp.mailgun.org
    EMAIL_PORT = 587
    EMAIL_HOST_USER =
    EMAIL_HOST_PASSWORD =
    EMAIL_USE_TLS = true
    ENABLE_PREVIEW_IMAGES = false
